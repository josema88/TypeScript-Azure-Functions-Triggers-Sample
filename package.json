{
  "name": "azure-functions-triggers-sample",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/data-tables": "^12.0.0-beta.2"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.2-beta2",
    "@types/azure": "^0.9.20",
    "@types/uuid": "^8.3.0",
    "typescript": "^3.3.3"
  }
}
